const myNum = 50;

// Both the below work in converting to string data type.
const myNumToString = String(myNum);
const myNumToString_2 = myNum.toString();

const myStr = '44';

// Both the below work in converting to number data type.
const myStrToNum = Number(myStr);
const myStrToNum_2 = parseInt(myStr);
